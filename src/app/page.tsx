import SearchContainer from '@/components/Search';
import TempContainer from '@/components/Temp';
export default async function Home() {
    return (
        <>
            <SearchContainer />
            <TempContainer />
        </>
    );
}
